# No real secrets live in this repo — the Anthropic key is a Worker secret,
# and the Supabase anon key in config.js is public by design (RLS protects it).
# This just keeps local noise out.
.DS_Store
node_modules/
.wrangler/
# local-only responsive test harness (loads index.html in phone-sized iframes)
_viewport_harness.html
*.log
.env
.env.*
.dev.vars*
!.dev.vars.example
!.env.example
